from setuptools import setup, find_packages

setup(
   name='file_system_analyzer',
   version='1.0',
   description='Parses command-line arguments and performs file system analysis',
   packages=find_packages(),
   install_requires=[],
)
